apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: goal-tracker-app
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/itsbaivab/Terraform-Full-Course-Azure.git  # Replace with your actual repository URL
    targetRevision: HEAD
    path: /manifest-files
  destination:
    server: https://kubernetes.default.svc
    namespace: goal-tracker
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
